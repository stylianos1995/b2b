# App
NODE_ENV=development
PORT=3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/b2b_mvp

# JWT (generate a secret for production)
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Platform admin (comma-separated emails; these users get platform_admin role)
# PLATFORM_ADMIN_EMAILS=admin@example.com

# Frontend (used for Stripe Checkout success/cancel redirects)
FRONTEND_URL=http://localhost:5173

# Stripe (required for Pay Invoice with Stripe Checkout)
# Get keys from https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_PUBLIC_KEY=pk_test_xxx
# Webhook signing secret from Stripe CLI or Dashboard → Developers → Webhooks
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# Notifications (optional; without SENDGRID_API_KEY, emails are logged only)
# SENDGRID_API_KEY=SG.xxx

# Events (optional: AMQP or Redis URL; leave empty for in-memory)
# EVENT_BUS_URL=amqp://localhost:5672
